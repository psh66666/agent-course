[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-course"
version = "0.1.0"
description = "A from-scratch agent learning project"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]
