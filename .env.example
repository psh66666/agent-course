# Copy these names into your shell environment when using a remote provider.
# The project uses MockModel when all three variables are absent.
MODEL_BASE_URL=https://your-provider.example/v1
MODEL_API_KEY=replace-with-your-key
MODEL_NAME=replace-with-your-model
